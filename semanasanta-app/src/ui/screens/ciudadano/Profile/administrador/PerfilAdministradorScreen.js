// TODO(iteración 2+): pantalla de perfil del rol Administrador. Vacía a
// propósito hasta que exista registro/login real para este rol (ver Usuario,
// Administrador en data/models y LoginScreen).
export function PerfilAdministradorScreen() {
  return null;
}

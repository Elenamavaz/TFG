// TODO(iteración 2+): pantalla de perfil del rol Junta de Cofradía. Vacía a
// propósito hasta que exista registro/login real para este rol (ver Usuario,
// MiembroJuntaCofradia en data/models y LoginScreen).
export function PerfilJuntaCofradiaScreen() {
  return null;
}

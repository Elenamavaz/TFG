// Cuenta de acceso del administrador: gestiona Ciudades y Juntas de Cofradías.
export class Administrador {
  constructor({ usuario, contrasena }) {
    this.usuario = usuario;
    this.contrasena = contrasena;
  }
}
